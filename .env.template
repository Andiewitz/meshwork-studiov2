# === DATABASES (docker handles these internally, don't change) ===
DATABASE_URL=postgresql://user:password@emnesh-workspace-db:5432/emnesh_workspace
WORKSPACE_DATABASE_URL=postgresql://user:password@emnesh-workspace-db:5432/emnesh_workspace
AUTH_DATABASE_URL=postgresql://user:password@emnesh-auth-db:5432/emnesh_auth

# === REDIS (docker handles this too) ===
REDIS_URL=redis://emnesh-redis:6379

# === APP ===
NODE_ENV=production
PORT=5000
APP_URL=http://52.90.236.186
FRONTEND_URL=http://52.90.236.186

# === SECRETS (generate these) ===
SESSION_SECRET=           # openssl rand -base64 32
JWT_SECRET=               # openssl rand -base64 64
ADMIN_SECRET=             # make something up, used for admin panel access

# === AI ===
OPENROUTER_API_KEY=       # your openrouter key

# === GOOGLE OAUTH (optional, leave blank if not using) ===
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# === SMTP (optional, leave blank to use console logging instead) ===
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
